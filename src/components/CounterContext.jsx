// pentru a utiliza Context API acesta trebuie mai intai importat
import { createContext } from "react";
export const CounterContext = createContext(null);

// context API are doua parti->un context provider si un context consumer
